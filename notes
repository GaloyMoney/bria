pending utxo

settled utxo -> with fee

batch -> looks up fee and takes it into account


settled utxo / batch

batch utxo lookup shows nothing
utxo in batch lookup shows nothing

utxo commit -> yup
batch commit -> false
with REPEATABLE READ batch tx


batch commit -> yup
utxo commit ->  yup
